# UP Actions 使用说明

UP Actions 是一个专门用于 Tauri 应用程序的 GitHub Actions 工作流集合，旨在自动化构建、打包和发布过程。它支持多种部署方式，包括本地文件复制和 FTP 上传。

## 目录结构

```
up-actions/
├── windows_tauri_release.yml  # Windows 平台 Tauri 发布工作流
├── src/
│   └── main.js               # 自定义 GitHub Action 主文件
└── README.md                 # 使用说明文档
```

## 功能特性

1. **版本自动提取**：从 `tauri.conf.json` 自动提取应用版本号
2. **文件复制**：将构建产物从源目录复制到版本化的发布目录
3. **FTP 上传支持**：支持三种 FTP 模式
   - 禁用 FTP（默认）
   - 外部 FTP 步骤（CI 模式）
   - 内置 FTP 上传（USE 模式）
4. **详细日志输出**：提供完整的操作日志和错误信息
5. **结果验证**：验证复制和上传的文件完整性

## 输入参数

| 参数名 | 描述 | 默认值 | 必需 |
|--------|------|--------|------|
| `source-dir` | 源目录，包含待复制的构建文件 | `src-tauri/target/release/bundle/` | 否 |
| `target-root` | 目标根目录，用于存放版本化发布文件 | `src-tauri/target/release/fabu/` | 否 |
| `config-file` | tauri 配置文件路径 | `src-tauri/tauri.conf.json` | 否 |
| `enable-ftp` | FTP 模式，可选值：`disabled`、`ci`、`use` | `disabled` | 否 |
| `ftp-host` | FTP 服务器地址（当 `enable-ftp` 为 `use` 时必需） | 空 | 否 |
| `ftp-username` | FTP 用户名（当 `enable-ftp` 为 `use` 时必需） | 空 | 否 |
| `ftp-password` | FTP 密码（当 `enable-ftp` 为 `use` 时必需） | 空 | 否 |
| `ftp-server-dir` | FTP 服务器目标目录 | 空 | 否 |

## 输出参数

| 参数名 | 描述 |
|--------|------|
| `version` | 从配置文件中提取的应用版本号 |
| `target-dir` | 版本化目标目录的完整路径 |
| `enable-ftp` | 当前使用的 FTP 模式 |
| `ftp-upload-success` | FTP 上传结果（`disabled`/`external`/`true`/`false`） |

## 使用方法

### 1. 准备工作

在使用 UP Actions 之前，需要：

1. 确保项目中有正确的 `tauri.conf.json` 配置文件
2. 如果使用 FTP 功能，需要在 GitHub 仓库中配置相应的 Secrets

### 2. 配置 Secrets

如果需要使用 FTP 功能，请在 GitHub 仓库的 Settings > Secrets and variables > Actions 中添加以下 Secrets：

| Secret 名称 | 描述 |
|-------------|------|
| `FTP_HOST` | FTP 服务器地址 |
| `FTP_USERNAME` | FTP 用户名 |
| `FTP_PASSWORD` | FTP 密码 |

### 3. 工作流配置示例

#### 示例 1：仅复制文件（禁用 FTP）

```yaml
name: Build and Release (No FTP)
on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy release files (No FTP)
        id: copy-files
        uses: ./src
        with:
          source-dir: src-tauri/target/release/bundle/
          target-root: src-tauri/target/release/fabu/
          config-file: src-tauri/tauri.conf.json
          # enable-ftp 默认为 'disabled'
```

#### 示例 2：使用外部 FTP 步骤

```yaml
name: Build and Release (External FTP)
on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy release files (Prepare for External FTP)
        id: copy-files
        uses: ./src
        with:
          source-dir: src-tauri/target/release/bundle/
          target-root: src-tauri/target/release/fabu/
          config-file: src-tauri/tauri.conf.json
          enable-ftp: ci

      - name: Deploy to FTP (External Step)
        if: steps.copy-files.outputs.enable-ftp == 'ci'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ${{ steps.copy-files.outputs.target-dir }}/
          server-dir: uploads/v${{ steps.copy-files.outputs.version }}/
```

#### 示例 3：使用内置 FTP 上传

```yaml
name: Build and Release (Built-in FTP)
on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy release files and upload via built-in FTP
        id: copy-and-upload
        uses: ./src
        with:
          source-dir: src-tauri/target/release/bundle/
          target-root: src-tauri/target/release/fabu/
          config-file: src-tauri/tauri.conf.json
          enable-ftp: use
          ftp-host: ${{ secrets.FTP_HOST }}
          ftp-username: ${{ secrets.FTP_USERNAME }}
          ftp-password: ${{ secrets.FTP_PASSWORD }}
          ftp-server-dir: uploads/v${{ steps.copy-and-upload.outputs.version }}/
```

#### 示例 4：完整的 Tauri 构建和发布工作流

```yaml
name: Windows Tauri Release
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [windows-latest]
    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"

      - name: Setup PNPM
        uses: pnpm/action-setup@v4
        with:
          version: 10.19.0
          run_install: false

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            src-tauri/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install frontend dependencies
        working-directory: .
        timeout-minutes: 5
        run: pnpm install --no-frozen-lockfile

      - name: Copy release files and upload via built-in FTP
        id: copy-and-upload
        uses: ./src
        with:
          source-dir: src-tauri/target/release/bundle/
          target-root: src-tauri/target/release/fabu/
          config-file: src-tauri/tauri.conf.json
          enable-ftp: use
          ftp-host: ${{ secrets.FTP_HOST }}
          ftp-username: ${{ secrets.FTP_USERNAME }}
          ftp-password: ${{ secrets.FTP_PASSWORD }}
          ftp-server-dir: uploads/v${{ steps.copy-and-upload.outputs.version }}/

      - name: Create Release
        if: steps.copy-and-upload.outputs.ftp-upload-success == 'true'
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ steps.copy-and-upload.outputs.target-dir }}/**
          draft: false
          prerelease: false
```

## FTP 模式详解

### disabled 模式（默认）

- 不执行任何 FTP 相关操作
- 仅完成文件复制到本地目标目录
- 适用于不需要自动上传的场景

### ci 模式

- 启用 FTP 功能但使用外部步骤处理上传
- 将文件复制到本地目标目录
- 输出参数指示需要外部 FTP 步骤
- 适用于需要特定 FTP 工具或配置的场景

### use 模式

- 启用并使用内置 FTP 上传功能
- 自动将文件上传到指定的 FTP 服务器
- 需要提供 FTP 服务器连接信息
- 适用于简单直接的 FTP 上传需求

## 故障排除

### 常见问题

1. **版本号未正确提取**
   - 检查 `tauri.conf.json` 是否存在且格式正确
   - 确认版本号位于 `version` 或 `package.version` 字段

2. **文件复制失败**
   - 检查源目录是否存在
   - 确认有足够的磁盘空间
   - 验证文件权限设置

3. **FTP 上传失败**
   - 检查 FTP 服务器连接信息是否正确
   - 确认网络连接正常
   - 验证 FTP 用户权限
   - 检查防火墙设置

### 日志查看

所有步骤都会输出详细日志，可在 Actions 页面查看每一步的执行情况和错误信息。关键日志包括：
- 版本号提取结果
- 目录创建状态
- 文件复制进度
- FTP 连接和上传状态

## 最佳实践

1. **版本管理**：确保 `tauri.conf.json` 中的版本号正确维护
2. **安全性**：敏感信息（如 FTP 密码）应始终使用 GitHub Secrets 存储
3. **测试**：在生产环境使用前，先在测试环境中验证工作流
4. **日志监控**：定期检查工作流执行日志，确保所有步骤正常执行
5. **备份策略**：重要发布文件应有适当的备份机制

通过以上配置和使用说明，您可以轻松地为 Tauri 应用程序建立自动化的构建和发布流程，根据需要选择最适合的 FTP 上传方式。
